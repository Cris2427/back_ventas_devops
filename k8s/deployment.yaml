apiVersion: apps/v1
kind: Deployment
metadata:
  name: back-ventas
spec:
  replicas: 2
  selector:
    matchLabels:
      app: back-ventas
  template:
    metadata:
      labels:
        app: back-ventas
        annotations:
          instrumentation.opentelemetry.io/inject-java: “false”
    spec:
      containers:
        - name: back-ventas
          image: criss247/back-ventas:latest
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "test"
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "750m"
              memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: back-ventas-service
spec:
  type: LoadBalancer
  selector:
    app: back-ventas
  ports:
    - port: 8080
      targetPort: 8080